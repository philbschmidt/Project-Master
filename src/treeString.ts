export class TreeString {
    _string: string;
    _categoryId: number;

    constructor(string: string, categoryId: number) {
        this._string = string;
        this._categoryId = categoryId;
    }
}